apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iine-grp.fullname" . }}-test-connection"
  labels:
{{ include "iine-grp.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "iine-grp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
